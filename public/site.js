const motion=document.querySelector('[data-motion-toggle]'),media=matchMedia('(prefers-reduced-motion:reduce)');
function update(){const stopped=document.documentElement.classList.contains('motion-paused');motion.textContent=stopped?'Resume motion':'Pause motion';motion.setAttribute('aria-pressed',String(stopped));}motion.addEventListener('click',()=>{document.documentElement.classList.toggle('motion-paused');update();});if(media.matches)document.documentElement.classList.add('motion-paused');update();
document.querySelectorAll('[data-start-deck]').forEach(el=>el.addEventListener('click',()=>{document.querySelector('[data-tape-deck]').scrollIntoView({behavior:media.matches?'instant':'smooth',block:'center'});if(document.querySelector('[data-tape-deck]').dataset.playing!=='true')document.querySelector('[data-session-play]').click();}));
