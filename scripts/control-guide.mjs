import {features} from '../public/guide-data.mjs';
export function controlGuide(){const first=features[0];return `<section class="tp-controls tp-guide" id="experience"><div class="tp-control-copy"><p class="tp-label">02 / GET TO KNOW YOUR INSTRUMENT</p><h2>PRESS<br><em>FOR PLAY.</em></h2><p>Record a sound. Let it repeat. Build on it with three more layers. TAPE puts a little loop studio under your hands—and the deck above is yours to play.</p><p class="tg-prompt">Choose a control. See what it does.</p><div class="tg-tabs" role="tablist" aria-label="Learn the TAPE controls">${features.map((f,i)=>`<button type="button" role="tab" id="guide-tab-${f.id}" aria-controls="guide-panel" aria-selected="${i===0}" tabindex="${i===0?0:-1}" data-guide-tab="${f.id}"><span>${String(i+1).padStart(2,'0')}</span>${f.label}</button>`).join('')}</div><p class="tg-keyboard">Mouse, touch or keyboard.<br>Arrow keys turn. Enter presses. Shift + Enter holds.</p></div><div class="tg-panel" id="guide-panel" role="tabpanel" aria-labelledby="guide-tab-play" tabindex="0"><div class="tg-photo" data-guide-photo><img src="/product/session-minimal-v8.webp" width="1448" height="1086" loading="lazy" alt="Photographic close-up of TAPE’s three blue transport keys" data-guide-image style="--zoom:${first.scale};--camera-x:${first.x}%;--camera-y:${first.y}%"><span class="tg-photo-id" aria-hidden="true">T—01 / <span data-guide-photo-number>01</span></span></div><div class="tg-explanation"><p class="tg-tag" data-guide-tag>${first.tag}</p><h3 data-guide-title>${first.title}</h3><p class="tg-description" data-guide-description>${first.description}</p><ol data-guide-steps>${first.steps.map(s=>`<li>${s}</li>`).join('')}</ol><a class="tg-try" href="#collection" data-guide-try="${first.id}">Find this on the deck <span aria-hidden="true">↗</span></a></div></div></section>`;}
